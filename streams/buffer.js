// represents a space on the computer's memory, used to transit data quickly
// *check typedArray / native js way to deal with binary

const buf = Buffer.from('hello world') // <- hexadecimal representation of each character
console.log(buf)